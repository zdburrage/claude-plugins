---
name: brand
description: Add a standalone branding system to the current project — config script, theme resolution, and env-based customization
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
  - Write
  - Edit
---

# Brand

Invoke the `app-branding` skill to detect the project's framework and UI library, then scaffold a complete branding system.

Pass any company name or LinkedIn URL from arguments to the skill workflow for pre-seeding the config script.
